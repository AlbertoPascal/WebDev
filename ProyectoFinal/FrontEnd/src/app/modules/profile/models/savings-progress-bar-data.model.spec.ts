import { SavingsProgressBarData } from './savings-progress-bar-data.model';

describe('SavingsProgressBarData', () => {
  it('should create an instance', () => {
    expect(new SavingsProgressBarData()).toBeTruthy();
  });
});
