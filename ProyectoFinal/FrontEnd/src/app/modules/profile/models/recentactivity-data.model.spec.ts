import { RecentactivityData } from './recentactivity-data.model';

describe('RecentactivityData', () => {
  it('should create an instance', () => {
    expect(new RecentactivityData()).toBeTruthy();
  });
});
